/*
const mapStateToProps = state => ({
    // state is the root reducer
    // calendar is a reducer in in the root reducer
    // date is the key in the calender reducer
    calendar: state.calendar.date
  });
    
  const mapDispatchToProps = dispatch => ({
    // setDate is the action function
    // dateObj is the action object payload
    calendar: dateObj => dispatch(setDate(dateObj))
  });
  
  export default connect(mapStateToProps, mapDispatchToProps)(App);
  */